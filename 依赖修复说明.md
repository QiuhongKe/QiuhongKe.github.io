# Jekyll网站依赖修复说明

## 问题摘要

您的Jekyll学术网站遇到了Ruby架构兼容性问题：
- 系统：macOS (Apple Silicon / M系列芯片)
- 问题：系统自带的Ruby 2.6.10在Rosetta模式下运行(x86_64)，导致原生扩展gems加载失败
- 错误示例：`cannot load such file -- google/protobuf_c`, `cannot load such file -- ffi_c`

## 已完成的工作

✅ 1. 修改了`Gemfile`，移除了版本限制
✅ 2. 创建了自动安装脚本`install-dependencies.sh`
✅ 3. 创建了详细的修复指南`SETUP_FIX.md`

## 推荐解决方案（按优先级）

### 🥇 方案1：使用自动安装脚本（最简单）

这个脚本会自动安装Homebrew和原生Ruby，然后安装所有依赖。

```bash
cd /Users/zinuo/Desktop/PhD/qiuhongke
bash install-dependencies.sh
```

脚本会做以下事情：
1. 检查并安装Homebrew（如果需要）
2. 安装Homebrew版本的Ruby（原生arm64）
3. 配置环境变量
4. 安装所有Jekyll依赖
5. 完成后您就可以运行`bash run.sh`启动网站

**注意**：安装过程中可能需要输入您的系统密码。

### 🥈 方案2：使用Docker（最可靠）

如果您有Docker Desktop：

```bash
cd /Users/zinuo/Desktop/PhD/qiuhongke
docker-compose up
```

网站将在 http://localhost:8080 运行

如果没有Docker，可以从这里下载：
https://www.docker.com/products/docker-desktop（选择Apple Silicon版本）

### 🥉 方案3：手动安装（如果自动脚本失败）

详细步骤请查看`SETUP_FIX.md`文件。

## 下一步操作

请选择一个方案执行：

1. **如果您想快速解决**：运行`bash install-dependencies.sh`
2. **如果您偏好Docker**：安装Docker Desktop后运行`docker-compose up`
3. **如果需要更多了解**：查看`SETUP_FIX.md`

## 安装后验证

安装成功后，运行：
```bash
bash run.sh
```

网站将在 http://localhost:4000 启动（如果使用Docker则是8080端口）

## 需要帮助？

如果遇到问题：
1. 检查`SETUP_FIX.md`中的详细说明
2. 确保按照步骤完成每一步
3. 重新打开终端确保环境变量生效

## 技术细节

- **根本原因**：macOS系统Ruby在Rosetta 2模式下运行
- **解决方法**：使用Homebrew安装的原生arm64 Ruby
- **影响的gems**：ffi, nokogiri, google-protobuf, sassc等需要原生扩展的gems

